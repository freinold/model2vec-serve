apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "model2vec-serve.fullname" . }}
  labels:
    {{- include "model2vec-serve.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "model2vec-serve.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "model2vec-serve.selectorLabels" . | nindent 8 }}
    spec:
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            {{- if .Values.models }}
            {{- range .Values.models }}
            - --model
            - {{ . | quote }}
            {{- end }}
            {{- if .Values.defaultModel }}
            - --default-model
            - {{ .Values.defaultModel | quote }}
            {{- end }}
            {{- else if .Values.model }}
            - --model
            - {{ .Values.model | quote }}
            {{- end }}
            {{- if .Values.modelOwner }}
            - --model-owner
            - {{ .Values.modelOwner | quote }}
            {{- end }}
            {{- if .Values.apiKey }}
            - --api-key
            - $(API_KEY)
            {{- end }}
            {{- range .Values.args }}
            - {{ . | quote }}
            {{- end }}
          env:
            - name: API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "model2vec-serve.fullname" . }}
                  key: api-key
                  optional: true
            {{- if .Values.modelAliases }}
            {{- $aliases := list }}
            {{- range .Values.modelAliases }}
            {{- $aliases = append $aliases (printf "%s=%s" .key .alias) }}
            {{- end }}
            - name: MODEL_ALIAS
              value: {{ $aliases | join "," | quote }}
            {{- end }}
            {{- if .Values.persistence.enabled }}
            - name: HOME
              value: {{ .Values.persistence.mountPath | quote }}
            {{- end }}
            {{- with .Values.env }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
          readinessProbe:
            httpGet:
              path: /ready
              port: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            {{- if .Values.persistence.enabled }}
            - name: models
              mountPath: {{ .Values.persistence.mountPath }}
            {{- end }}
            {{- with .Values.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
      volumes:
        {{- if .Values.persistence.enabled }}
        - name: models
          persistentVolumeClaim:
            claimName: {{ .Values.persistence.existingClaim | default (printf "%s-models" (include "model2vec-serve.fullname" .)) }}
        {{- end }}
        {{- with .Values.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
