apiVersion: v1
kind: Service
metadata:
  name: {{ include "model2vec-serve.fullname" . }}
  labels:
    {{- include "model2vec-serve.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "model2vec-serve.selectorLabels" . | nindent 4 }}
